/*package ch.epfl.javions.demodulation;

import org.junit.jupiter.api.Test;

import java.io.*;

import static org.junit.Assert.assertArrayEquals;
import static org.junit.Assert.assertThrows;

public class PowerComputerTest {

    @Test
     void throwsOnIllegalBatchSize() throws IOException {
        DataInputStream stream = new DataInputStream(new BufferedInputStream(
                new FileInputStream(new File("resources/samples.bin"))));;
        assertThrows(IllegalArgumentException.class, () -> PowerComputer test = new PowerComputer(stream, -7));
    }

    @Test

}*/
